import React from "react";

const LoginForm = props => {
  return (
    <form>
      <input type="text" />
      <input type="text" />
      <button type="submit"></button>
    </form>
  );
};

export default LoginForm;
